#include<bits/stdc++.h>
using namespace std;
// brute force approach : create an another temp array 
// in which you store the non zero (lets say x elements) and then at the
// end push those n-x number of zeroes



 void pushZerosToEnd(vector<int>& arr) {
        
        
    }



